import { styled } from "styled-components";

export const ChatsLayoutStyled = styled.main`
  padding: 12px;
  height: calc(100vh - 24px);
  display: flex;
  gap: 12px;
  margin: 0;
  overflow: clip;
`;
